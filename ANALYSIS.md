# MoodCash 产品分析文档

---

## 产品定位

MoodCash 是一款**情绪记账**微信小程序。

与传统记账工具不同，MoodCash 的核心理念是：**记录花钱时的心情，理解消费背后的情绪**。

**一句话定位：** 不只是记账，更是记录你的消费情绪故事。

**核心差异化：**
- 传统记账 → 记录"花了多少"
- MoodCash → 记录"花钱时的感受" + "花了多少"

**平台：** 微信小程序
**技术栈：** Vue 3 + Pinia（uni-app 编译）
**当前版本：** v2.2.0

---

## 用户群体

### 主要用户
- **年龄：** 20-35岁中国年轻职场人
- **特征：** 日常使用微信，偏好轻量工具而非独立App
- **需求：** 想了解"为什么花钱"，不只是"花了什么"
- **心理：** 希望对自己的消费习惯有更深的自我认知

### 次要用户
- **在校大学生：** 记录日常开销，顺便关注情绪状态

### 用户心智
> "我想看到我的消费故事，不只是一个表格。"

---

## 设计目标

| 目标 | 说明 |
|------|------|
| **有温度** | 每个交互都应该温暖，不像填表 |
| **零学习成本** | 任何人都能在10秒内记一笔账 |
| **视觉精致** | 必须像原生App，不像学生课程作业 |
| **愉悦的微交互** | 细腻的动画奖励用户操作，但不拖慢节奏 |
| **有灵魂的数据** | 图表不只是数字，要讲出情绪故事 |

---

## 页面结构图

```
首页
├── Hero情绪卡片（今日心情emoji + 今日总支出）
├── 今日消费时间轴
│   ├── 时间轴分隔线（日期标签）
│   └── 账单卡片列表（分类icon + 名称 + 金额 + 备注）
├── 浮动记账按钮（FAB，右下紫色圆形"+"）
└── 版本更新弹窗（首次打开时）

记账页
├── 金额显示区（大字展示当前输入金额）
├── 分类选择网格（3×3，9个emoji分类）
├── 心情选择器（5个emoji情绪选项）
├── 备注输入框
├── 图片上传区（最多9张）
├── 时间选择器
└── 数字键盘（底部固定）

时间轴页
├── 搜索栏（按分类名/备注/金额搜索）
├── 分类筛选标签（横向滚动，9个分类）
├── 按日期分组的账单列表
│   ├── 日期头（日期 + 当日总支出）
│   └── 账单卡片（同首页样式）
└── 空状态（无记录时）

统计页
├── Hero月度总览卡片（月总支出 + 趋势对比）
├── 月份选择器（左右翻页）
├── 分类排行榜（Top分类 + 进度条 + 金额）
├── 情绪分析卡片（情绪消费占比 + 洞察文字）
├── 情绪日历（日历格子 + 情绪emoji标注）
└── 时段消费图（按时间段的柱状图）

我的页
├── Hero个人信息卡片（头像emoji + 用户名 + 累计统计）
├── 菜单列表
│   ├── 预算管理
│   ├── 消费日历
│   ├── 月度报告
│   ├── 主题切换
│   └── 清除数据（红色警告）
└── 版本号

账单详情页
├── Hero区域（心情emoji + 金额 + 分类）
├── 信息卡片（分类/日期/时间/备注）
├── 图片展示区（可点击大图预览）
└── 操作按钮（编辑 / 删除）

预算管理页
├── Hero预算卡片（预算金额 + 已用进度条 + 剩余）
├── 预算选项（1000/2000/3000/5000/8000/10000）
├── 自定义输入
└── 预算状态信息卡片

日历页
├── 月份导航（左右翻页）
├── 情绪日历网格（每天显示心情emoji）
├── 月度汇总卡片（本月心情统计）
└── 选中日期详情

月度报告页
├── 深色主题报告卡片（大emoji + 月总支出 + 心情标签）
├── Top分类排行
├── 情绪洞察
└── 操作按钮（分享 / 返回）
```

---

## 页面跳转关系图

```
┌─────────────────────────────────────────────────────┐
│                    底部TabBar导航                      │
│  ┌──────┐  ┌──────┐  ┌──────┐  ┌──────┐            │
│  │ 首页  │  │时间轴│  │ 统计  │  │ 我的  │            │
│  └──┬───┘  └──┬───┘  └──┬───┘  └──┬───┘            │
│     │         │         │         │                  │
│     │         │         │    ┌────┴────┐             │
│     │         │         │    │         │             │
│     │         │         │    ▼         ▼             │
│     │         │         │ 预算管理   消费日历          │
│     │         │         │           月度报告          │
│     │         │         │           主题切换          │
│     │         │         │                            │
│     ├─────────┼─────────┼──── 点击账单 ──→ 账单详情   │
│     │         │         │                  │         │
│     │         │         │              ┌───┴───┐     │
│     │         │         │              ▼       ▼     │
│     │         │         │            编辑     删除    │
│     │         │         │              │       │     │
│     │         │         │              ▼       │     │
│     │         │         │           记账页     │     │
│     │         │         │              ▲       │     │
│     │         │         │              │       │     │
│     └─────────┴─────────┴── FAB按钮 ───┘       │     │
│                                                │     │
│                              返回上一页 ◀───────┘     │
└─────────────────────────────────────────────────────┘
```

**跳转规则：**
- 首页/时间轴/统计/我的 → 通过底部TabBar切换
- 任意页面 → 点击FAB"记一笔" → 记账页（模态弹出）
- 任意账单列表 → 点击账单 → 账单详情页
- 账单详情 → 编辑 → 记账页（数据回填）
- 我的 → 预算管理/日历/月报 → 对应子页面
- 所有子页面 → 左上角返回按钮 → 回到上级页面

---

## 核心用户流程

### 流程一：记一笔账（核心流程）

```
用户点击FAB"+"按钮
  ↓
进入记账页（底部弹出动画）
  ↓
输入金额（数字键盘）
  ↓
选择分类（emoji网格，如🍜餐饮）
  ↓
选择心情（5个emoji，如😊开心）
  ↓
可选：输入备注、上传照片、调整时间
  ↓
点击确认
  ↓
返回上一页
  ↓
账单出现在时间轴中（渐入动画）
  ↓
如果超出预算 → 弹出预算提醒
```

### 流程二：查看与管理账单

```
首页/时间轴 → 看到账单列表
  ↓
点击某笔账单
  ↓
进入详情页（查看金额/分类/心情/备注/照片）
  ↓
选择操作：
  ├── 编辑 → 跳转记账页（数据预填）→ 保存 → 返回
  └── 删除 → 确认弹窗 → 删除 → 返回列表
```

### 流程三：分析消费习惯

```
进入统计页
  ↓
查看本月总支出 + 与上月对比趋势
  ↓
下拉查看分类排行榜（哪个分类花最多）
  ↓
查看情绪分析（哪种心情下花钱最多）
  ↓
查看情绪日历（哪天情绪最差/最好）
  ↓
查看时段分析（哪个时间段花钱最多）
  ↓
获得消费洞察（自动生成的文字建议）
```

---

## 当前UI问题

### 一眼看出是"学生项目"的地方

| 问题 | 具体表现 | 严重程度 |
|------|---------|---------|
| **TabBar图标** | 已修复：emoji → SVG图标 + 弹跳动画 | ✅ 已解决 |
| **页面无动画** | 已修复：全部页面加入场动画 | ✅ 已解决 |
| **时间轴卡片平庸** | 已修复：重构布局，层次更清晰 | ✅ 已解决 |
| Section标题颜色混乱 | 分类排行用橙色，时段图用黄色，其他用紫色，无逻辑 | 🔲 待修复 |
| 删除弹窗是系统原生样式 | `wx.showModal` 的标准确认/取消，没有品牌感 | 🔲 待修复 |
| 空状态太机械 | 大emoji + 文字，没有插画和引导感 | 🔲 待修复 |
| 详情页信息平铺 | 所有字段同等大小，金额不突出 | 🔲 待修复 |
| 金额数字没有动画 | 添加账单后金额直接跳变，没有滚动效果 | 🔲 待修复 |
| 进度条没有入场动画 | 首次显示就是最终宽度，没有从0填充的效果 | 🔲 待修复 |
| 情绪色条太细 | 8rpx宽的竖条在高分屏几乎看不见 | 🔲 待修复 |

### 设计一致性问题

| 问题 | 说明 |
|------|------|
| `#666` 色值出现 | timeline.wxss中filter chip用了`#666`，与全局色阶`#1a1a2e/#4a4a68/#9e9eb8`不统一 |
| 危险色不统一 | 有的地方用`#ff6b6c`做边框，有的做背景，透明度值不一致 |
| 圆角不一致 | TimelineItem用16rpx，其他卡片用24rpx，没有明确的层级规则 |
| 渐变方向不统一 | Hero卡片都是135deg，月报卡片是160deg |

---

## 期望升级方向

### 短期（下个版本）

| 方向 | 说明 |
|------|------|
| 统一Section标题颜色 | 全部使用品牌紫`#6C63FF`，取消橙色和黄色 |
| 详情页金额突出 | 金额放大到72rpx居中，其他信息弱化 |
| 自定义删除弹窗 | 替换系统`wx.showModal`，使用品牌风格弹窗 |
| 空状态优化 | 增加小插画/插图，文案更温暖 |
| 进度条入场动画 | 首次显示从0宽度动画到实际值 |

### 中期

| 方向 | 说明 |
|------|------|
| 金额数字滚动动画 | 数字变化时每位独立滚动 |
| 情绪色条加宽 | 8rpx → 12rpx，增加圆角 |
| 富文本空状态 | 每个页面的空状态有独特插画和引导文案 |
| 骨架屏加载 | 数据加载时显示shimmer占位符 |

### 长期

| 方向 | 说明 |
|------|------|
| 页面转场动画 | 列表→详情的共享元素过渡 |
| 手势操作 | 左滑账单显示快捷操作 |
| 数据可视化升级 | 更精致的图表和动画 |
| 深色模式 | 完整的深色主题支持 |

---

*分析基于 MoodCash v2.2.0 代码库。*
